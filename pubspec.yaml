name: fmpc_tracker
description: "A new Flutter parcel tracking project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  cloud_firestore: ^4.15.8
  mobile_scanner: ^5.0.0
  excel: ^4.0.0
  path_provider: ^2.1.2
  share_plus: ^8.0.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
